using System;

namespace Srp
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //Errado, fora do escopo principal
            static void CreateClient()
            {

            }

            //Errado, fora do escopo principal
            static void ReadClient()
            {

            }

            //Errado, fora do escopo principal
            static void UpdateClient()
            {

            }
            //Errado, fora do escopo principal
            static void DeleteClient()
            {

            }

            //Errado, fora do escopo principal
            static void NotifyClient()
            {

            }
        }
    }
}
